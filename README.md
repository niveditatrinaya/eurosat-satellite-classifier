# eurosat-satellite-classifier
Fine-tuning a pretrained ResNet50 to classify satellite images into land-use types (forest, farmland, urban, etc.) using the EuroSAT dataset, built as a foundational project in computer vision and transfer learning.
